node_modules/
.turbo/
dist/
.output/
.vinxi/
.vite/
.tanstack/
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store
*.log
coverage/
/apps/web/src/routeTree.gen.ts

# Local-disk storage driver's dev/CI attachment files (see packages/storage) —
# never real data, and STORAGE_DRIVER=gcs is what production actually uses.
.data/

# GCS service-account key mounted into web/worker by infra/docker-compose.yml —
# real one is a secret; local-only stub is still not something to track.
/infra/secrets/
